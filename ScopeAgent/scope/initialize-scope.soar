sp {propose*initialize-scope
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> +)
   (<o> ^name initialize-scope)
}

sp {apply*initialize-scope
   (state <s> ^operator <op>)
   (<op> ^name initialize-scope)
-->
   (<s> ^name scope
        ^desired <d>)
   (<d> ^better lower)
}

